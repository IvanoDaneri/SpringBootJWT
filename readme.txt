Project SpringBootRest description
This is a SpringBoot application that exposes rest services (GET and POST) with
two different security configurations that we'll describe later (see ldapAuth branch).
